.container
  %header
    %h1.h1
      All Games
      %small.thin.light (Readonly)

  %table.table-standard.full-width
    %thead
      %tr
        %th img
        %th= sortable "purchase_date"
        %th= sortable "game_name"
        %th= sortable "system"
        %th Includes
        %th Alternate Name
        %th Actions
    %tbody
      - @games.each do |game|
        = render "game_line", game: game

= render "shared/modal"